version: "2"
linters:
  disable:
    # Disabling deprecated linters
    - exportloopref
    # To fix later
    - dupl
    - forcetypeassert
    # Disabling by decision
    - depguard
    - exhaustruct
    - gofumpt
    - prealloc
    - tagliatelle

